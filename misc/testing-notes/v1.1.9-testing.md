# Adaire Blocks v1.1.9 – Testing Checklist

## 1. Our Process Block
- [ ] Verify desktop/tablet/mobile column controls update immediately in the editor and on the frontend.
- [ ] Confirm grid gap slider respects device-specific values.
- [ ] Add/remove/reorder process steps and ensure IDs persist after save.
- [ ] Test icon picker + link fields (including `/relative` URLs with “open in new tab”).
- [ ] Check hover states, padding, and border settings render in both editor and frontend.

## 2. Content Toggle Block
- [ ] Add 3+ toggles, rename pills, and reorder them.
- [ ] Confirm pill styles (background, text, border, radius, padding) apply to both inactive and active states.
- [ ] Switch between toggles and confirm GSAP animation respects duration/ease settings.
- [ ] Test toggle position “top” and “bottom”.
- [ ] Validate container mode + max-width sliders for each breakpoint.
- [ ] Confirm block works with nested content (images, buttons, headings) inside each toggle panel.

## 3. Swiper Carousel + Swiper Slide
- [ ] Insert carousel and multiple slide blocks; ensure slide toolbar only appears inside the carousel.
- [ ] Test media picker, overlay type (color vs gradient), headline/body fields, and responsive background options per slide.
- [ ] Adjust carousel height, border radius, speed, loop, autoplay delay, and effect; verify on frontend.
- [ ] Confirm draggable cursor overlay text/color renders and updates.
- [ ] Check keyboard navigation and swipe gestures on desktop (mouse drag) and mobile (touch).

## 4. Testimonial 2 Block
- [ ] Add multiple testimonials with portrait + logo images; confirm uploader previews update instantly.
- [ ] Validate navigation buttons (size, hover, arrows) and keyboard accessibility (Tab/Enter/Space).
- [ ] Adjust typography controls (description, name, title) and ensure responsive values apply for tablet/mobile.
- [ ] Test background/card color controls along with quote image opacity/size sliders.
- [ ] Confirm carousel speed + spacing controls render correctly.

## 5. Icon Refresh
- [ ] Spot-check block inserter icons for each tier (Free, Plus, Premium).
- [ ] Open Adaire → Block Management and confirm icons render with full color (no black squares).
- [ ] Build plugin (`npm run build`) and ensure compiled assets include the refreshed icons.

## 6. Regression Pass
- [ ] Smoke test previously shipping blocks (Modal, Industries, Particles, Services) to ensure no console errors.
- [ ] Verify `npm run build` completes and `npm run build:free` (if applicable) still strips premium-only assets.
- [ ] Confirm readme.txt/README.md stable tag and changelog reflect 1.1.9.

